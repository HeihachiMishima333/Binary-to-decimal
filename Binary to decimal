//המרה בין בסיס בינארי לעשרוני
#include <stdio.h>
#include <math.h>

void main() {
    int a, b, count = 0, i, sum = 0, p = 0;
    printf("Enter a binary number:");
    scanf("%d", & a); 
   
    
    while (a != 0) {
      if (a % 10 == 1)
        sum += pow(2, p);
      p++;
      a = a / 10;
    }
    printf("The decimal value is=%d", sum);

  }
